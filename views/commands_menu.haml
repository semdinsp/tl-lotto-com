.panel.panel-success
  .panel-heading 
    = I18n.translate('commands.title')
  .panel-body
    %p Commands to get entries
    - heads = ["command","result"] 
    - vals= [{cmd: "4D",res: I18n.translate('commands.4d')},{cmd: "4D x x x x", res: I18n.translate('commands.4dxxxx')}]
    %table(class="table table-hover table-condensed")
      %tr
        - heads.each do |h|
          %th=h
      - vals.each do |v|
        %tr
          %td=v[:cmd]
          %td=v[:res]